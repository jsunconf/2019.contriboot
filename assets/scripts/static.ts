export const PROPOSALS = 'contributions';
export const INTERESTS= 'interests';

export const isProposal = (type: string) => type === PROPOSALS;
export const isInterest = (type: string) => type === INTERESTS;
